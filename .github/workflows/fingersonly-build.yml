name: Fingers Only Build
on:
  push:
    branches: [arrowrip-build]
    paths:
      - 'fingersonly/**'
      - '.github/workflows/fingersonly-build.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.2.0'
      - name: Build
        working-directory: fingersonly
        run: gradle build --stacktrace
      - uses: actions/upload-artifact@v4
        with:
          name: FingersOnly-Fabric-1.21.11
          path: fingersonly/build/libs/*.jar
