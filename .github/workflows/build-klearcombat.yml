name: Build KlearCombat Forge 1.20.1

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "CI temporarily paused while v2.0.2 source chunks are uploaded"
